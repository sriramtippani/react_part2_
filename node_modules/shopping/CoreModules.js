var http = require("http");
var fs = require("fs");
var data = require("./data.json");

// var server = http.createServer(function (req, res) {
//     // console.log(req.server);
//     // console.log("*********");
//     res.setHeader('Access-Control-Allow-Origin', '*');
//     res.write(JSON.stringify(data));
//     res.end();
// });

// server.listen(1256);
var filename = 'app.js';
fs.readFile(filename, function (err, data) {
    if (err) throw err;
    console.log(data.toString());
});
var newFile = "sample.txt";
fs.appendFile(newFile, data, function (err) {
    if (err) console.log(err);
    console.log("Appended");

})


var dir = "../NODEJSDEMO";
let len = 0;
fs.readdir(dir, function (err, files) {
    for (let i = 0, len = files.length; i < len; i++) {
        const fileinfo = files[i];
        fs.stat(fileinfo, function (err, result) {
            console.log(fileinfo);
            console.log(result);
            console.log("**************************");
        });
    }
});
var os = require('os');

console.log(os.type());

console.log(os.uptime())







var events = require("events");

var ev = new events.EventEmitter();// Create the object of event emitter.


//define events..

ev.on("myEvent", function (data) {
   data();
});
function callMe() {
    console.log('Hi');
};
ev.emit("myEvent", callMe);

